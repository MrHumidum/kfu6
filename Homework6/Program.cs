using System;

namespace Homework6
{
    class Program
    {
        static void Main(string[] args)
        {
            // Оператор мобильной связи
            //             Написать программу, содержащую решение следующих
            // задач: Вы получили тему, по которой должны составить классы.
            // Цель задания: Написать программу, в которой реализованы все принципы ООП. Для этого
            // необходимо реализовать не менее 4 классов:
            // ● Один должен быть абстрактным
            // ● Должно быть не менее 2 наследников
            // ● Не менее 5 методов в каждом классе (необходимо показать свойство на
            // чтение и на чтение-запись)
            // ● Не менее 4 свойств
            // ● Должно быть не менее 2 конструкторов в классах наследниках

            // В Main создать объекты классов, показать работу методов. В программе для ваших
            // объектов используйте какие-то данные, который необходимо читать с файла/введены с
            // клавиатуры/прописано в коде. Например, класс человек, создаете экземпляры для
            // человека, а имена, фамилии читаете с файла/вводите с клавиатуры/заранее прописано в коде .
            // Необходимо быть готовым к тому, что каждый принцип нужно объяснять. Ваша программа
            // должна объяснять человеку то, о чем у вас тема и показать основные моменты вашей
            // темы, то, что считаете важным.
            Tariff basicTariff = new Tariff("Базовый", 300, 10, 100, 100);
            Tariff premiumTariff = new Tariff("Премиум", 1000, 50, 500, 500);

            MobileOperator operator1 = new MobileOperator("МегаСвязь", 85.5, 500, new List<Tariff> { basicTariff, premiumTariff });

            for (int i = 0; i < 1000; i++)
            {
                operator1.AddClient();
            }

            DateTime birthDate = new DateTime(1990, 5, 15);
            MobileClient client1 = new MobileClient("Иван", "Иванов", birthDate, "Мужской", "1234567890", basicTariff, 500);

            Support support1 = new Support("Елена", "Соколова", new DateTime(1985, 3, 20), "Женский", "Менеджер", 5, "Техническая поддержка", true);

            Console.WriteLine("Информация об операторе:");
            operator1.DisplayInfo();
            Console.WriteLine($"\nДоход оператора: {operator1.CalculateIncome():C}");

            Console.WriteLine("\nИнформация о клиенте:");
            client1.DisplayInfo();

            Console.WriteLine("\nРабота службы поддержки:");
            support1.AssistClient("Проблемы с тарифом");

            client1.ChangeTariff(premiumTariff);
            Console.WriteLine("\nКлиент сменил тариф:");
            client1.DisplayInfo();
        }

    }

}
